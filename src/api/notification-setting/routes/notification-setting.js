'use strict';

/**
 * notification-setting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::notification-setting.notification-setting');
