'use strict';

/**
 * subscription-plan service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::subscription-plan.subscription-plan');
