'use strict';

/**
 * verification-document router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::verification-document.verification-document');
