'use strict';

/**
 * verification-document service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::verification-document.verification-document');
