'use strict';

/**
 * checklist-organizer router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::checklist-organizer.checklist-organizer');
